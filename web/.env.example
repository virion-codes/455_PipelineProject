# Supabase — Database → Connection string → URI
# Copy to .env.local and replace [YOUR-PASSWORD] with your database password.
# The Next.js app currently uses SQLite (../data/shop.db). Use this when you
# point the app or other tools at Postgres.
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.phadvdfahjcatkajurzr.supabase.co:5432/postgres"
