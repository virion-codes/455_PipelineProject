# Supabase — Database → Connection string → URI
# Copy to .env.local, then edit that file in your editor (do not paste edit
# instructions into the terminal — zsh treats [...] as globs).
# Replace YOUR_PASSWORD below with your Supabase database password.
# The Next.js app currently uses SQLite (../data/shop.db). Use this when you
# point the app or other tools at Postgres.
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@db.phadvdfahjcatkajurzr.supabase.co:5432/postgres"
